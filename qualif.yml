---
# This playbook install whole qualif bench

- name: apply qualif configuration
  hosts: qualif
  remote_user: qualif
  become_user: root
  become_method: su

  roles:
    - database
    - phpmyadmin
    - streammanager
    - standalone
    - nas
    - serviio
    - crontab
